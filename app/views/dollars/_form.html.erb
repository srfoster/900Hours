<%= form_for(@dollar) do |f| %>
  <% if @dollar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dollar.errors.count, "error") %> prohibited this dollar from being saved:</h2>

      <ul>
      <% @dollar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :donation_id %><br />
    <%= f.number_field :donation_id %>
  </div>
  <div class="field">
    <%= f.label :hour_id %><br />
    <%= f.number_field :hour_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
